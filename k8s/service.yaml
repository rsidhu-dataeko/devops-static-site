apiVersion: v1
kind: Service
metadata:
  name: devops-site
  namespace: devops-site

spec:
  selector:
    app: devops-site
  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: NodePort

